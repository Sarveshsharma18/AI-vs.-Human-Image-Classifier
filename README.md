# AI-vs.-Human-Image-Classifier
AI vs. Human image classifier that uses pretrained diffusion model components (VAEs and U-Net noise predictors) as feature extractors, comparing which representation best detects AI-generated images.
